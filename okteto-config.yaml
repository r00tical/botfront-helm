botfront:
  app:
    # The complete external host of the Botfront application (eg. botfront.yoursite.com). It must be set even if running on a private or local DNS (it populates the ROOT_URL).
    host: botfront-r00tical.cloud.okteto.com

mongodb:
  enabled: false # disable to use an external mongoDB host
  mongodbUsername: bf
  mongodbPassword: passw0rd
  mongodbHostsAndPorts: cluster0.kyvbq.mongodb.net
  mongodbDatabase: virtualcoach
